import React, { useState, useEffect, useRef } from 'react';
import { 
  Calculator, 
  Cpu, 
  Code, 
  Briefcase, 
  GraduationCap, 
  Mail, 
  Phone, 
  MapPin, 
  ChevronDown,
  TrendingUp,
  Video,
  Monitor
} from 'lucide-react';

// --- 3D Interactive Card Component ---
const TiltCard = ({ children, className = '' }) => {
  const cardRef = useRef(null);
  const [transform, setTransform] = useState('perspective(1000px) rotateX(0deg) rotateY(0deg) scale3d(1, 1, 1)');
  const [isHovered, setIsHovered] = useState(false);

  const handleMouseMove = (e) => {
    if (!cardRef.current) return;
    const rect = cardRef.current.getBoundingClientRect();
    const x = e.clientX - rect.left;
    const y = e.clientY - rect.top;
    const centerX = rect.width / 2;
    const centerY = rect.height / 2;
    
    // Calculate rotation (-10 to 10 degrees)
    const rotateX = ((y - centerY) / centerY) * -10;
    const rotateY = ((x - centerX) / centerX) * 10;

    setTransform(`perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) scale3d(1.02, 1.02, 1.02)`);
  };

  const handleMouseLeave = () => {
    setIsHovered(false);
    setTransform('perspective(1000px) rotateX(0deg) rotateY(0deg) scale3d(1, 1, 1)');
  };

  return (
    <div
      ref={cardRef}
      onMouseMove={handleMouseMove}
      onMouseEnter={() => setIsHovered(true)}
      onMouseLeave={handleMouseLeave}
      style={{ transform, transition: isHovered ? 'none' : 'transform 0.5s ease-out' }}
      className={`relative rounded-2xl bg-white/5 border border-white/20 backdrop-blur-xl p-6 shadow-[0_8px_32px_rgba(0,0,0,0.3)] overflow-hidden will-change-transform group ${className}`}
    >
      {/* Continuous Glassy Sliding Sheen on Hover */}
      <div className="absolute top-0 -inset-full h-full w-1/2 z-0 block transform -skew-x-12 bg-gradient-to-r from-transparent via-white/20 to-transparent opacity-0 group-hover:opacity-100 group-hover:animate-[slidingGlass_1.5s_ease-in-out_infinite] pointer-events-none mix-blend-overlay" />

      {/* Dynamic glare effect */}
      {isHovered && (
        <div 
          className="absolute inset-0 pointer-events-none rounded-2xl mix-blend-overlay"
          style={{
            background: 'linear-gradient(105deg, transparent 20%, rgba(255,255,255,0.1) 25%, transparent 30%)',
            animation: 'glare 2s infinite linear'
          }}
        />
      )}
      {children}
    </div>
  );
};

export default function App() {
  const [mousePos, setMousePos] = useState({ x: 0, y: 0 });
  const [scrollY, setScrollY] = useState(0); // Added scroll state for Earth rotation

  // Global mouse tracking for background parallax and Intersection Observer
  useEffect(() => {
    const handleGlobalMouseMove = (e) => {
      setMousePos({
        x: (e.clientX / window.innerWidth - 0.5) * 40,
        y: (e.clientY / window.innerHeight - 0.5) * 40,
      });
    };
    window.addEventListener('mousemove', handleGlobalMouseMove);

    // Add scroll listener for the Earth rotation
    const handleScroll = () => {
      setScrollY(window.scrollY);
    };
    window.addEventListener('scroll', handleScroll);

    // Intersection Observer for scroll animations
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add('is-visible');
          }
        });
      },
      { threshold: 0.1, rootMargin: "0px 0px -50px 0px" }
    );

    document.querySelectorAll('.reveal').forEach((el) => observer.observe(el));

    return () => {
      window.removeEventListener('mousemove', handleGlobalMouseMove);
      window.removeEventListener('scroll', handleScroll);
      observer.disconnect();
    };
  }, []);

  return (
    <div className="min-h-screen bg-[#0a0a0a] text-slate-200 font-sans selection:bg-cyan-500/30 overflow-x-hidden relative">
      
      {/* 3D Parallax Background */}
      <div 
        className="fixed inset-0 pointer-events-none opacity-40 z-0 transition-transform duration-300 ease-out"
        style={{
          transform: `translate(${mousePos.x}px, ${mousePos.y}px)`,
          backgroundImage: `
            radial-gradient(circle at 15% 50%, rgba(6, 182, 212, 0.15), transparent 25%),
            radial-gradient(circle at 85% 30%, rgba(139, 92, 246, 0.15), transparent 25%)
          `
        }}
      />
      
      {/* Cyber Grid */}
      <div className="fixed inset-0 pointer-events-none z-0" 
           style={{ 
             backgroundImage: 'linear-gradient(rgba(255,255,255,0.03) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,0.03) 1px, transparent 1px)', 
             backgroundSize: '50px 50px',
             transform: 'perspective(500px) rotateX(60deg) translateY(-100px) translateZ(-200px)',
             opacity: 0.2
           }} 
      />

      {/* Rotating Earth Background */}
      <div 
        className="fixed top-1/2 left-1/2 w-[700px] h-[700px] md:w-[1100px] md:h-[1100px] rounded-full pointer-events-none z-0 opacity-[0.25] mix-blend-screen"
        style={{
          backgroundImage: `url('https://raw.githubusercontent.com/mrdoob/three.js/master/examples/textures/planets/earth_atmos_2048.jpg')`,
          backgroundSize: 'auto 100%',
          backgroundRepeat: 'repeat-x',
          backgroundPositionX: `${scrollY * 0.8}px`, // 3D Scroll Rotation effect
          boxShadow: 'inset -120px -120px 120px #0a0a0a, inset 20px 20px 80px rgba(6, 182, 212, 0.4)', // Inner shadow creates spherical depth
          filter: 'grayscale(70%) sepia(40%) hue-rotate(160deg) brightness(0.9) contrast(1.2)', // Cyber/Hologram color grading
          transform: `translate(calc(-50% + ${mousePos.x * 0.4}px), calc(-50% + ${mousePos.y * 0.4}px)) rotate(-15deg)`, // Tilt and subtle mouse parallax
          willChange: 'background-position, transform'
        }}
      />

      {/* Main Content */}
      <div className="relative z-10 max-w-6xl mx-auto px-6 py-12 md:py-24">
        
        {/* --- HERO SECTION --- */}
        <header className="min-h-[80vh] flex flex-col justify-center items-start pt-10">
          <div className="inline-block mb-4 px-4 py-1.5 rounded-full border border-cyan-500/30 bg-cyan-500/10 text-cyan-400 text-sm font-semibold tracking-wide backdrop-blur-md">
            Available for Immediate Joining
          </div>
          <h1 className="text-5xl md:text-7xl font-extrabold tracking-tight mb-6 bg-clip-text text-transparent bg-gradient-to-r from-cyan-400 via-blue-500 to-purple-600 drop-shadow-[0_0_15px_rgba(6,182,212,0.5)]">
            Tushar Verma
          </h1>
          <h2 className="text-2xl md:text-4xl font-light text-slate-400 mb-8 max-w-2xl">
            Bridging the precision of <span className="text-white font-medium">Accounting</span> with the innovation of <span className="text-white font-medium">Technology & Hardware</span>.
          </h2>
          
          <div className="flex flex-wrap gap-4 mb-12">
            <a href="#contact" className="px-8 py-3 rounded-lg bg-cyan-600 hover:bg-cyan-500 text-white font-medium transition-all shadow-[0_0_20px_rgba(6,182,212,0.4)] hover:shadow-[0_0_30px_rgba(6,182,212,0.6)] transform hover:-translate-y-1">
              Hire Me
            </a>
            <a href="#experience" className="px-8 py-3 rounded-lg bg-white/5 hover:bg-white/10 border border-white/10 text-white font-medium backdrop-blur-md transition-all transform hover:-translate-y-1">
              View Experience
            </a>
          </div>

          {/* 3D Floating Badges */}
          <div className="flex flex-wrap gap-6 mt-8">
            <TiltCard className="p-4 flex items-center gap-3">
              <Calculator className="text-cyan-400 w-6 h-6" />
              <span className="font-medium text-sm">ERP & Bookkeeping</span>
            </TiltCard>
            <TiltCard className="p-4 flex items-center gap-3">
              <Cpu className="text-purple-400 w-6 h-6" />
              <span className="font-medium text-sm">Hardware Engineering</span>
            </TiltCard>
            <TiltCard className="p-4 flex items-center gap-3">
              <Code className="text-blue-400 w-6 h-6" />
              <span className="font-medium text-sm">Flutter & Python</span>
            </TiltCard>
          </div>

          <div className="absolute bottom-10 left-1/2 -translate-x-1/2 animate-bounce opacity-50">
            <ChevronDown className="w-8 h-8" />
          </div>
        </header>

        {/* --- SKILLS SECTION --- */}
        <section id="skills" className="py-20 border-t border-white/10 reveal slide-up">
          <h3 className="text-3xl font-bold mb-10 flex items-center gap-3">
            <Monitor className="text-cyan-500" /> Technical Arsenal
          </h3>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
            <TiltCard>
              <h4 className="text-xl font-bold text-cyan-400 mb-4 border-b border-white/10 pb-2">Finance & Accounts</h4>
              <ul className="space-y-3 text-slate-300">
                <li className="flex items-center gap-2"><div className="w-1.5 h-1.5 rounded-full bg-cyan-500"/> GST Reporting & Compliance</li>
                <li className="flex items-center gap-2"><div className="w-1.5 h-1.5 rounded-full bg-cyan-500"/> ERP Software Management</li>
                <li className="flex items-center gap-2"><div className="w-1.5 h-1.5 rounded-full bg-cyan-500"/> Bank Reconciliation</li>
                <li className="flex items-center gap-2"><div className="w-1.5 h-1.5 rounded-full bg-cyan-500"/> High-Volume Journal Entries</li>
                <li className="flex items-center gap-2"><div className="w-1.5 h-1.5 rounded-full bg-cyan-500"/> Advanced Microsoft Excel</li>
              </ul>
            </TiltCard>

            <TiltCard>
              <h4 className="text-xl font-bold text-purple-400 mb-4 border-b border-white/10 pb-2">IT & Hardware</h4>
              <ul className="space-y-3 text-slate-300">
                <li className="flex items-center gap-2"><div className="w-1.5 h-1.5 rounded-full bg-purple-500"/> Hardware Troubleshooting</li>
                <li className="flex items-center gap-2"><div className="w-1.5 h-1.5 rounded-full bg-purple-500"/> System Maintenance</li>
                <li className="flex items-center gap-2"><div className="w-1.5 h-1.5 rounded-full bg-purple-500"/> Tech Support (Zero Downtime)</li>
                <li className="flex items-center gap-2"><div className="w-1.5 h-1.5 rounded-full bg-purple-500"/> Inventory Management</li>
                <li className="flex items-center gap-2"><div className="w-1.5 h-1.5 rounded-full bg-purple-500"/> Typing Speed: 40-43 WPM</li>
              </ul>
            </TiltCard>

            <TiltCard>
              <h4 className="text-xl font-bold text-blue-400 mb-4 border-b border-white/10 pb-2">Dev & Projects</h4>
              <ul className="space-y-3 text-slate-300">
                <li className="flex items-center gap-2"><div className="w-1.5 h-1.5 rounded-full bg-blue-500"/> Flutter Cross-Platform Dev</li>
                <li className="flex items-center gap-2"><div className="w-1.5 h-1.5 rounded-full bg-blue-500"/> Python Programming</li>
                <li className="flex items-center gap-2"><div className="w-1.5 h-1.5 rounded-full bg-blue-500"/> 3D Game Engine Basics</li>
                <li className="flex items-center gap-2"><div className="w-1.5 h-1.5 rounded-full bg-blue-500"/> Stock Market Analytics</li>
                <li className="flex items-center gap-2"><div className="w-1.5 h-1.5 rounded-full bg-blue-500"/> Digital Video Production</li>
              </ul>
            </TiltCard>
          </div>
        </section>

        {/* --- EXPERIENCE SECTION --- */}
        <section id="experience" className="py-20 border-t border-white/10 reveal slide-up">
          <h3 className="text-3xl font-bold mb-12 flex items-center gap-3">
            <Briefcase className="text-purple-500" /> Professional Experience
          </h3>
          
          <div className="space-y-8 relative before:absolute before:inset-0 before:ml-5 before:-translate-x-px md:before:mx-auto md:before:translate-x-0 before:h-full before:w-0.5 before:bg-gradient-to-b before:from-transparent before:via-white/20 before:to-transparent">
            
            {/* Job 1 */}
            <div className="relative flex items-center justify-between md:justify-normal md:odd:flex-row-reverse group reveal slide-left">
              <div className="flex items-center justify-center w-10 h-10 rounded-full border border-white/10 bg-[#0a0a0a] shadow-[0_0_15px_rgba(6,182,212,0.5)] shrink-0 md:order-1 md:group-odd:-translate-x-1/2 md:group-even:translate-x-1/2 z-10">
                <Calculator className="w-5 h-5 text-cyan-400" />
              </div>
              <TiltCard className="w-[calc(100%-3rem)] md:w-[calc(50%-3rem)] p-6 !bg-cyan-950/10">
                <div className="flex justify-between items-start mb-2">
                  <h4 className="text-xl font-bold text-white">Accountant</h4>
                  <span className="text-xs font-semibold text-cyan-400 bg-cyan-400/10 px-2 py-1 rounded">2 Years</span>
                </div>
                <p className="text-cyan-300/80 text-sm mb-4 font-medium">GSSV Associates / IT Sector • New Delhi</p>
                <ul className="text-slate-300 text-sm space-y-2">
                  <li>• Managed end-to-end bookkeeping and financial record-keeping using ERP software to ensure data integrity.</li>
                  <li>• Processed GST filings and maintained compliance with updated tax regulations.</li>
                  <li>• Performed monthly bank reconciliations and recorded high-volume journal entries.</li>
                  <li>• Streamlined the invoicing process for sales and purchases, improving payment tracking.</li>
                </ul>
              </TiltCard>
            </div>

            {/* Job 2 */}
            <div className="relative flex items-center justify-between md:justify-normal md:odd:flex-row-reverse group reveal slide-right">
              <div className="flex items-center justify-center w-10 h-10 rounded-full border border-white/10 bg-[#0a0a0a] shadow-[0_0_15px_rgba(168,85,247,0.5)] shrink-0 md:order-1 md:group-odd:-translate-x-1/2 md:group-even:translate-x-1/2 z-10">
                <Cpu className="w-5 h-5 text-purple-400" />
              </div>
              <TiltCard className="w-[calc(100%-3rem)] md:w-[calc(50%-3rem)] p-6 !bg-purple-950/10">
                <div className="flex justify-between items-start mb-2">
                  <h4 className="text-xl font-bold text-white">Hardware Engineer</h4>
                  <span className="text-xs font-semibold text-purple-400 bg-purple-400/10 px-2 py-1 rounded">6 Months</span>
                </div>
                <p className="text-purple-300/80 text-sm mb-4 font-medium">IT Services • New Delhi</p>
                <ul className="text-slate-300 text-sm space-y-2">
                  <li>• Provided technical support and resolved hardware-related issues to minimize operational downtime.</li>
                  <li>• Managed hardware inventory and performed routine maintenance on essential business equipment.</li>
                </ul>
              </TiltCard>
            </div>

          </div>
        </section>

        {/* --- EDUCATION & PROJECTS --- */}
        <section className="py-20 border-t border-white/10 grid grid-cols-1 lg:grid-cols-2 gap-10 reveal slide-up">
          
          {/* Education */}
          <div>
            <h3 className="text-2xl font-bold mb-8 flex items-center gap-3">
              <GraduationCap className="text-blue-400" /> Education
            </h3>
            <div className="space-y-4">
              <div className="p-5 rounded-xl bg-white/5 border border-white/10 hover:bg-white/10 transition-colors">
                <h4 className="text-lg font-bold text-white">Bachelor of Computer Applications (BCA)</h4>
                <p className="text-blue-300 text-sm mb-2">OM Sterling Global University</p>
                <span className="text-xs bg-blue-500/20 text-blue-300 px-2 py-1 rounded">Pursuing</span>
              </div>
              <div className="p-5 rounded-xl bg-white/5 border border-white/10 hover:bg-white/10 transition-colors">
                <h4 className="text-lg font-bold text-white">Class XII (Senior Secondary)</h4>
                <p className="text-slate-400 text-sm">CBSE Board • Completed</p>
              </div>
              <div className="p-5 rounded-xl bg-white/5 border border-white/10 hover:bg-white/10 transition-colors">
                <h4 className="text-lg font-bold text-white">Class X (Secondary)</h4>
                <p className="text-slate-400 text-sm">CBSE Board • Completed</p>
              </div>
            </div>
          </div>

          {/* Projects */}
          <div>
            <h3 className="text-2xl font-bold mb-8 flex items-center gap-3">
              <Code className="text-cyan-400" /> Projects & Interests
            </h3>
            <div className="space-y-4">
              <TiltCard className="!p-5">
                <div className="flex items-center gap-3 mb-2">
                  <div className="p-2 bg-cyan-500/20 rounded-lg"><Code className="w-5 h-5 text-cyan-400"/></div>
                  <h4 className="text-lg font-bold text-white">Application Development</h4>
                </div>
                <p className="text-slate-300 text-sm">Developing cross-platform applications and 3D game engines utilizing Flutter and Python.</p>
              </TiltCard>
              
              <TiltCard className="!p-5">
                <div className="flex items-center gap-3 mb-2">
                  <div className="p-2 bg-purple-500/20 rounded-lg"><TrendingUp className="w-5 h-5 text-purple-400"/></div>
                  <h4 className="text-lg font-bold text-white">Market Analysis</h4>
                </div>
                <p className="text-slate-300 text-sm">Monitoring Indian stock market trends and global commodity prices to inform financial outlooks.</p>
              </TiltCard>

              <TiltCard className="!p-5">
                <div className="flex items-center gap-3 mb-2">
                  <div className="p-2 bg-blue-500/20 rounded-lg"><Video className="w-5 h-5 text-blue-400"/></div>
                  <h4 className="text-lg font-bold text-white">Digital Content</h4>
                </div>
                <p className="text-slate-300 text-sm">Managing branding and video production workflow for creative digital projects.</p>
              </TiltCard>
            </div>
          </div>
        </section>

        {/* --- CONTACT SECTION --- */}
        <section id="contact" className="py-20 border-t border-white/10 text-center reveal slide-up">
          <h3 className="text-4xl font-bold mb-6">Let's Build Something Great</h3>
          <p className="text-slate-400 max-w-xl mx-auto mb-10">
            Whether you need a meticulous accountant to handle complex reconciliations or a tech-savvy hardware/software engineer, I'm ready to bring immediate value to your team.
          </p>
          
          <div className="flex flex-col md:flex-row justify-center items-center gap-6">
            <a href="mailto:mv7429@gmail.com" className="group relative px-6 py-4 rounded-xl bg-white/5 border border-white/10 overflow-hidden flex items-center gap-3 w-full md:w-auto hover:border-cyan-500/50 transition-colors">
              <div className="absolute inset-0 bg-gradient-to-r from-cyan-500/10 to-blue-500/10 opacity-0 group-hover:opacity-100 transition-opacity" />
              <Mail className="text-cyan-400 relative z-10" />
              <span className="font-medium text-white relative z-10">mv7429@gmail.com</span>
            </a>
            
            <a href="tel:+919818154461" className="group relative px-6 py-4 rounded-xl bg-white/5 border border-white/10 overflow-hidden flex items-center gap-3 w-full md:w-auto hover:border-purple-500/50 transition-colors">
              <div className="absolute inset-0 bg-gradient-to-r from-purple-500/10 to-pink-500/10 opacity-0 group-hover:opacity-100 transition-opacity" />
              <Phone className="text-purple-400 relative z-10" />
              <span className="font-medium text-white relative z-10">+91 9818154461</span>
            </a>

            <div className="px-6 py-4 rounded-xl bg-white/5 border border-white/10 flex items-center gap-3 w-full md:w-auto">
              <MapPin className="text-slate-400" />
              <span className="font-medium text-slate-300">New Delhi, India</span>
            </div>
          </div>
        </section>

        <footer className="py-8 text-center text-slate-500 text-sm border-t border-white/5 mt-10">
          <p>© {new Date().getFullYear()} Tushar Verma. All rights reserved.</p>
        </footer>

      </div>
      
      {/* CSS for custom animations */}
      <style dangerouslySetInnerHTML={{__html: `
        @keyframes glare {
          0% { transform: translateX(-100%) translateY(-100%) rotate(45deg); }
          100% { transform: translateX(200%) translateY(200%) rotate(45deg); }
        }
        @keyframes slidingGlass {
          0% { transform: translateX(-150%) skewX(-15deg); }
          100% { transform: translateX(250%) skewX(-15deg); }
        }
        
        /* Reveal Animations */
        .reveal {
          opacity: 0;
          will-change: transform, opacity;
        }
        .reveal.is-visible {
          opacity: 1;
        }
        .reveal.slide-up.is-visible {
          animation: slideUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
        }
        .reveal.slide-left.is-visible {
          animation: slideLeft 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
        }
        .reveal.slide-right.is-visible {
          animation: slideRight 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
        }
        
        @keyframes slideUp {
          from { opacity: 0; transform: translateY(50px); }
          to { opacity: 1; transform: translateY(0); }
        }
        @keyframes slideLeft {
          from { opacity: 0; transform: translateX(-50px); }
          to { opacity: 1; transform: translateX(0); }
        }
        @keyframes slideRight {
          from { opacity: 0; transform: translateX(50px); }
          to { opacity: 1; transform: translateX(0); }
        }
      `}} />
    </div>
  );
}
